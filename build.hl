// golang
// odin
// crystal
// liblua5.4-dev

> cd source-code
> cd backend
> odin build main.odin -file -extra-linker-flags:"-static -lm -ldl" -o:speed
> cd ..
> cd tui
> go get bph-tui
> go build
> cd ..
> cd cli
> crystal build src/main.cr --release
> sudo mv main bph
